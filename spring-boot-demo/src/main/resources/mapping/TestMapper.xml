<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bin.mapper.TestMapper">
 
 
 <select id="seelctUserInfo" resultType="hashMap">
 	select * from advisory_user_info
 </select>
 
   <select id="selectModelByquId" resultType="hashMap" parameterType="String">
  	SELECT a.begin_sentences beginSen,
  		a.end_sentences endSentences from 
  		message_order m LEFT JOIN answer_template a 
  		ON m.answer_template_id = a.id where m.out_order_no
  		 = #{questionId} 
  		 and m.parent_order_id is NULL
  </select>
 
</mapper>